# Pinned lockfile — free-tier builds resolve fresh each time, so unpinned ranges let a
# new upstream release break pip's resolver (httpx/httpcore backtracking → build fail).
# Versions below are a known-good resolved set. Regenerate if upgrading.

# --- web app ---
fastapi==0.136.3
starlette==1.2.1
uvicorn==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic_core==2.46.4

# --- http stack (the cluster that broke the build) ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
certifi==2026.5.20
idna==3.18
sniffio==1.3.1

# --- tts / media ---
edge-tts==7.2.8
aiohttp==3.14.1
imageio==2.37.3
imageio-ffmpeg==0.6.0
pillow==12.2.0
numpy==2.4.6

# --- publish layer: YouTube Data API v3 ---
google-api-python-client==2.197.0
google-auth==2.53.0
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0
httplib2==0.31.2
requests==2.34.2
requests-oauthlib==2.0.0
oauthlib==3.3.1
